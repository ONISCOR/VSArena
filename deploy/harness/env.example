# Copy to .env on the Oracle VM (never commit .env).
# Same HARNESS_INGEST_SECRET as Vercel Production.

HARNESS_DOMAIN=harness.example.com
VSARENA_APP_URL=https://www.vsarena.app
HARNESS_INGEST_SECRET=
# Same Ed25519 private key as Vercel (quoted PEM with \n, concatenated, or raw PKCS8 base64). Public key is on GET /health.
# VSARENA_RESULTS_ED25519_PRIVATE=
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
